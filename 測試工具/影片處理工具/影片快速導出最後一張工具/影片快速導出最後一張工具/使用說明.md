# 影片快速導出最後一張工具

版本 1.0.1（2026-09-05）

首次執行 fix_package.bat，以 Python 3 建立 .venv 並安裝 requirements.txt；之後執行 run_tool.bat。輸入 MP4/MOV/AVI/MKV/WebM/M4V，實際可用格式依 OpenCV 解碼器；輸出 PNG/JPG 到指定資料夾。同名輸出增加 _2、_3，不覆寫既有檔案。取消於目前解碼呼叫返回後生效；不會移除已完成成果。

本版改善：修正管理器啟動入口；採背景處理與取消，同名輸出自動編號，依賴改為工具專用環境。
