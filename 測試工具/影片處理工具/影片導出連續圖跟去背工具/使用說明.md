# 影片導出連續圖跟去背工具

版本 1.0.1（2026-09-05）

Chrome/Edge 開啟 video_sequence_bgremover_tool.html。輸入可解碼、具有有限片長的影片；輸出 PNG 或 ZIP。張數 1–300、單邊最多 8192、單張最多 1600 萬像素；批次原始像素預算 512 MiB，超過請分批或縮小尺寸。JSZip 優先使用內附版本。去背為色鍵/亮度與 Alpha 運算，非 AI 語意去背；中性的 Alpha 清理保留原透明度。

本版改善：修正 Alpha 清理重複乘透明度；新增匯出互斥/取消、像素預算、定位逾時與縮圖資源回收。
